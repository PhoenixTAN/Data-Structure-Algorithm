#include <stdio.h>
#include <stdlib.h>

char* convert(char* s, int numRows) {

    char z_string[] = "";
    int i = 0;
    for( i = 0; s[i] != '\0'; i++ ) {

    }

    return z_string;
}

int main()
{
    char s1[] = "LEETCODEISHIRING";
    int numRows = 3;
    convert( s1, numRows );

    return 0;
}
